Declarations{
Types: A, B, C;
Modules{
        math{
        A exp(NUMBER);
        VAR ceil(A);
        }
        builtins{
        print(VAR);
        }
    }
}
A first;
var second;

first = exp(22);
second = first;